name: Avantiqo Code Abort Known Bad Proof

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled-carrier:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Temporary carrier disabled before restoration'
