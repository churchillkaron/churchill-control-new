name: DISABLED - Avantiqo Intelligence E2E Repair 20260831

on:
  workflow_dispatch:
permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy Intelligence repair is disabled
        run: |
          echo "Avantiqo Intelligence is LOCAL_ONLY."
          echo "This legacy repair workflow cannot rewrite provider source, create external leases, commit code, deploy, or submit provider work."
          echo "Use current local-only tests and owned Node01 runtime contracts instead."
