name: DISABLED - Avantiqo Intelligence Modal Certify V7

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: External Intelligence compute is disabled
        run: |
          echo "Avantiqo Intelligence is LOCAL_ONLY."
          echo "This legacy external-compute workflow cannot deploy, certify, diagnose, prewarm, or submit provider work."
          echo "Use the owned local Intelligence runtime and Node01 readiness contracts instead."
