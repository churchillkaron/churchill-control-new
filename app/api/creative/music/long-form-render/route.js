import crypto from "node:crypto";
import {NextResponse} from "next/server";
import {requireOrganizationAccess} from "@/lib/auth/runtime/OrganizationAccessRuntime";
import * as CreativeProjectRepository from "@/lib/creative/projects/repositories/CreativeProjectRepository";
import {buildMusicReleaseRenderPlan} from "@/lib/creative/music/runtime/CreativeMusicReleaseRenderPlanRuntime";
import {buildAudioPostLanguageSession} from "@/lib/creative/music/runtime/CreativeAudioPostVersionRuntime";
import {buildProfessionalAudioLongFormRenderPlan} from "@/lib/creative/music/runtime/CreativeProfessionalAudioLongFormRenderPlanRuntime";
import {inspectProfessionalAudioLongFormWorker,submitProfessionalAudioLongFormWorkerJob,pollProfessionalAudioLongFormWorkerJob} from "@/lib/creative/music/runtime/CreativeProfessionalAudioLongFormWorkerRuntime";
const PERMISSIONS=Object.freeze(["creative.execute","creative.production.run","creative.*"]),KEY="music_multitrack_project";const text=v=>String(v??"").trim();
function canonical(v){if(Array.isArray(v))return v.map(canonical);if(!v||typeof v!=="object")return v;return Object.fromEntries(Object.keys(v).sort().map(k=>[k,canonical(v[k])]));}function fingerprint(v){return crypto.createHash("sha256").update(JSON.stringify(canonical(v))).digest("hex");}
export async function POST(request){try{const body=await request.json(),organizationId=text(body.organization_id),projectId=text(body.creative_project_id);if(!organizationId||!projectId)return NextResponse.json({success:false,error:"organization_id and creative_project_id required"},{status:400});const access=await requireOrganizationAccess({organizationId,request,requiredAnyPermission:PERMISSIONS});if(!access.success)return NextResponse.json({success:false,error:access.error||"forbidden"},{status:access.status||403});const project=await CreativeProjectRepository.getById(projectId);if(!project||String(project.organization_id)!==String(organizationId))throw new Error("CREATIVE_AUDIO_LONG_FORM_PROJECT_NOT_FOUND");const base=project.metadata?.[KEY];if(!base)throw new Error("CREATIVE_AUDIO_LONG_FORM_SESSION_REQUIRED");const session=body.delivery_language?buildAudioPostLanguageSession(base,body.delivery_language):base,releasePlan=buildMusicReleaseRenderPlan(session,body.options||{}),graphFingerprint=fingerprint({session,releasePlan}),longFormPlan=buildProfessionalAudioLongFormRenderPlan({session,release_plan:releasePlan,graph_fingerprint:graphFingerprint,chunk_seconds:body.chunk_seconds}),worker=inspectProfessionalAudioLongFormWorker(),action=text(body.action||"plan").toLowerCase();if(action==="start"){if(!releasePlan.long_form_delivery?.server_render_required)throw new Error("CREATIVE_AUDIO_LONG_FORM_SERVER_RENDER_NOT_REQUIRED");if(!worker.ready)return NextResponse.json({success:false,error:worker.blocking_reason,worker,long_form_plan:longFormPlan,provider_job_submitted:false,endpoint_mutation_performed:false},{status:409,headers:{"Cache-Control":"no-store"}});const submission=await submitProfessionalAudioLongFormWorkerJob({organization_id:organizationId,creative_project_id:projectId,plan:longFormPlan,graph_fingerprint:graphFingerprint,session_snapshot:session});return NextResponse.json({success:true,worker,long_form_plan:longFormPlan,job_id:submission.job_id,job_hash:submission.job_hash,pending:submission.pending,worker_response:submission.response,provider_job_submitted:false,endpoint_mutation_performed:false},{status:200,headers:{"Cache-Control":"no-store"}});}if(action==="status"){const result=await pollProfessionalAudioLongFormWorkerJob({job_id:body.job_id,job_hash:body.job_hash});return NextResponse.json({success:true,worker,result,provider_job_submitted:false,endpoint_mutation_performed:false},{status:200,headers:{"Cache-Control":"no-store"}});}return NextResponse.json({success:true,release_plan:{contract:releasePlan.contract,project_revision:releasePlan.project_revision,duration_seconds:releasePlan.duration_seconds,sample_rate:releasePlan.sample_rate,channels:releasePlan.channels,channel_layout:releasePlan.channel_layout,delivery_profile:releasePlan.delivery_profile,long_form_delivery:releasePlan.long_form_delivery},graph_fingerprint:graphFingerprint,long_form_plan:longFormPlan,worker,provider_job_submitted:false,endpoint_mutation_performed:false},{status:200,headers:{"Cache-Control":"no-store"}});}catch(error){return NextResponse.json({success:false,error:error?.message||"Long-form audio render planning failed",provider_job_submitted:false,endpoint_mutation_performed:false},{status:error?.status||400});}}
