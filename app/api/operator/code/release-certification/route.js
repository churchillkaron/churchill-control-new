import { createCodeAIReleaseCertificationCapability } from "@/lib/platform/capabilities/createCodeAIReleaseCertificationCapability";
import { requireOrganizationAccess } from "@/lib/platform/security/requireOrganizationAccess";
export const runtime="nodejs"; export const maxDuration=300; const REQUIRED_PERMISSION="platform.deploy.production";
function text(v,n=1000){return String(v??"").trim().slice(0,n)}
export async function POST(request){try{const body=await request.json().catch(()=>({}));const organizationId=text(body.organizationId||body.organization_id,200);if(!organizationId)return Response.json({success:false,error:"organizationId required"},{status:400});const access=await requireOrganizationAccess({organizationId,request,requiredPermission:REQUIRED_PERMISSION});if(!access.success)return Response.json({success:false,error:access.error},{status:access.status||403});const context={organizationId,actor:{id:access.user?.id||access.userId,email:access.user?.email||null},permissions:access.permissions||[],callerRequest:request,metadata:{source:"CODE_STUDIO",explicit_release_certification_request:true}};const capability=createCodeAIReleaseCertificationCapability();capability.authorize({context});const result=await capability.execute({context,payload:body});return Response.json({success:true,result})}catch(error){return Response.json({success:false,error:text(error?.message||error,1000)},{status:error?.status||500})}}
