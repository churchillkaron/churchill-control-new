"use client";

import { useEffect } from "react";

export default function Home() {
  useEffect(() => {
    // safe place for anything dynamic later
  }, []);

  return (
    <div>
      {/* your landing UI stays EXACTLY the same */}
    </div>
  );
}