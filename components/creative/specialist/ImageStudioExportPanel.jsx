"use client";
import { useState } from "react";
export default function ImageStudioExportPanel({workspace,persistence}){const [format,setFormat]=useState("PNG");const [busy,setBusy]=useState(false);const board=workspace.artboards.find(x=>x.id===workspace.selection.artboard_id);return <section className="mt-5"><div className="text-[9px] font-semibold uppercase tracking-[.22em] text-white/28">Output</div><div className="mt-2 flex gap-2"><select value={format} onChange={e=>setFormat(e.target.value)} className="rounded-md border border-white/[.07] bg-black/30 px-2 py-1.5 text-[9px] text-white/55"><option>PNG</option><option>JPEG</option><option>PDF</option></select><button disabled={busy||!board} onClick={async()=>{setBusy(true);try{const r=await fetch('/api/workspace/creative/image-studio/export',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({organization_id:workspace.organization_id,project_id:workspace.project_id,artboard_id:board.id,format})});const b=await r.blob();if(!r.ok)throw new Error('Export failed');const u=URL.createObjectURL(b);const a=document.createElement('a');a.href=u;a.download=`${board.name||'image-studio'}.${format==='JPEG'?'jpg':format.toLowerCase()}`;a.click();URL.revokeObjectURL(u);}finally{setBusy(false)}}} className="rounded-md border border-[#D6A66A]/20 bg-[#D6A66A]/[.06] px-3 py-1.5 text-[9px] text-[#D6A66A] disabled:opacity-30">{busy?'Rendering…':`Export ${format}`}</button></div></section>}
