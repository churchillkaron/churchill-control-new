"use client";

function Field({label,value,onChange,onFocus,onBlur,type="number",step="1"}) {
  return <label className="block"><span className="text-[8px] uppercase tracking-[.14em] text-white/25">{label}</span><input type={type} step={step} value={value??""} onFocus={onFocus} onBlur={onBlur} onChange={e=>onChange(type==="number"?Number(e.target.value):e.target.value)} className="mt-1 w-full rounded-md border border-white/[.07] bg-black/30 px-2 py-1.5 text-[10px] text-white/60 outline-none focus:border-[#D6A66A]/40"/></label>;
}
function SelectField({label,value,onChange,options}) {
  return <label className="block"><span className="text-[8px] uppercase tracking-[.14em] text-white/25">{label}</span><select value={value} onChange={e=>onChange(e.target.value)} className="mt-1 w-full rounded-md border border-white/[.07] bg-black/30 px-2 py-1.5 text-[10px] text-white/60 outline-none focus:border-[#D6A66A]/40">{options.map(item=><option key={item} value={item}>{item}</option>)}</select></label>;
}

export default function ImageStudioLayerInspector({workspace}) {
  const id=workspace.selection.layer_ids[0]; const layer=workspace.layers.find(x=>x.id===id); if(!layer)return null;
  const b=layer.bounds||{},t=layer.transform||{},s=layer.style||{},c=layer.content||{},m=layer.metadata||{},crop=m.crop||{};
  const patchBounds=(key,v)=>workspace.updateLayerLocal(id,{bounds:{...b,[key]:v}});
  const patchStyle=(key,v)=>workspace.updateLayerLocal(id,{style:{...s,[key]:v}});
  const patchCrop=(key,v)=>workspace.updateLayerLocal(id,{metadata:{...m,crop:{...crop,[key]:v}}});
  const align=(mode)=>workspace.alignSelected(mode);
  const transactionProps={onFocus:workspace.beginHistoryTransaction,onBlur:workspace.endHistoryTransaction};
  return <section className="mt-5"><div className="text-[9px] font-semibold uppercase tracking-[.22em] text-[#D6A66A]/62">Inspector</div>
    <div className="mt-3 grid grid-cols-2 gap-2"><Field {...transactionProps} label="X" value={b.x??0} onChange={v=>patchBounds("x",v)}/><Field {...transactionProps} label="Y" value={b.y??0} onChange={v=>patchBounds("y",v)}/><Field {...transactionProps} label="Width" value={b.width??240} onChange={v=>patchBounds("width",Math.max(24,v))}/><Field {...transactionProps} label="Height" value={b.height??180} onChange={v=>patchBounds("height",Math.max(24,v))}/><Field {...transactionProps} label="Rotate" value={t.rotation??0} onChange={v=>workspace.updateLayerLocal(id,{transform:{...t,rotation:v}})}/></div>
    <div className="mt-2 grid grid-cols-3 gap-1"><button onClick={()=>align("left")} className="rounded border border-white/[.06] py-1 text-[8px] text-white/35">Left</button><button onClick={()=>align("center_x")} className="rounded border border-white/[.06] py-1 text-[8px] text-white/35">Center</button><button onClick={()=>align("right")} className="rounded border border-white/[.06] py-1 text-[8px] text-white/35">Right</button></div>
    {layer.layer_type==="IMAGE"?<div className="mt-3 space-y-2"><div className="text-[8px] uppercase tracking-[.14em] text-white/25">Non-destructive crop</div><div className="grid grid-cols-2 gap-2"><Field {...transactionProps} label="Focal X %" value={Math.round((crop.x??.5)*100)} onChange={v=>patchCrop("x",Math.max(0,Math.min(1,v/100)))}/><Field {...transactionProps} label="Focal Y %" value={Math.round((crop.y??.5)*100)} onChange={v=>patchCrop("y",Math.max(0,Math.min(1,v/100)))}/><Field {...transactionProps} label="Zoom %" value={Math.round((crop.zoom??1)*100)} onChange={v=>patchCrop("zoom",Math.max(1,v/100))}/><Field {...transactionProps} label="Radius" value={m.mask_radius??0} onChange={v=>workspace.updateLayerLocal(id,{metadata:{...m,mask_radius:Math.max(0,v)}})}/></div></div>:null}
    {layer.layer_type==="TEXT"?<div className="mt-3 space-y-2"><Field {...transactionProps} label="Text" type="text" value={c.text||""} onChange={v=>workspace.updateLayerLocal(id,{content:{...c,text:v}})}/><Field {...transactionProps} label="Font family" type="text" value={s.font_family||"Arial, Helvetica, sans-serif"} onChange={v=>patchStyle("font_family",v)}/><div className="grid grid-cols-2 gap-2"><Field {...transactionProps} label="Font size" value={s.font_size??52} onChange={v=>patchStyle("font_size",Math.max(6,v))}/><Field {...transactionProps} label="Weight" value={s.font_weight??600} onChange={v=>patchStyle("font_weight",v)}/><Field {...transactionProps} label="Line height" step="0.05" value={s.line_height??1.05} onChange={v=>patchStyle("line_height",v)}/><Field {...transactionProps} label="Letter spacing" step="0.1" value={s.letter_spacing??0} onChange={v=>patchStyle("letter_spacing",v)}/><SelectField label="Align" value={s.text_align||"left"} onChange={v=>patchStyle("text_align",v)} options={["left","center","right"]}/><SelectField label="Vertical" value={s.vertical_align||"top"} onChange={v=>patchStyle("vertical_align",v)} options={["top","middle","bottom"]}/></div><label className="block"><span className="text-[8px] uppercase tracking-[.14em] text-white/25">Color</span><input type="color" value={s.color||"#111111"} onFocus={workspace.beginHistoryTransaction} onBlur={workspace.endHistoryTransaction} onChange={e=>patchStyle("color",e.target.value)} className="mt-1 h-8 w-full rounded-md border border-white/[.07] bg-black/30"/></label></div>:null}
  </section>;
}
