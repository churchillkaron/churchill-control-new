const CONTRACT="AVANTIQO_MUSIC_SOUND_DESIGN_PREPARATION_V1";
function text(v){return String(v??"").trim();}
export function evaluateMusicSoundDesignPreparation(soundObject={}){const source=(soundObject.layers||[]).find(layer=>text(layer.asset_id))?.asset_id||null,prep=soundObject.source_preparation||{},vehicle=soundObject.vehicle_acoustics||{},failures=[];if(!source)failures.push("SOUND_DESIGN_SOURCE_ASSET_REQUIRED");if(!text(prep.prepared_asset_id))failures.push("SOUND_DESIGN_DRY_PREP_REQUIRED");if(source&&text(prep.parent_source_asset_id)!==text(source))failures.push("SOUND_DESIGN_DRY_PREP_SOURCE_STALE");if(soundObject.category==="VEHICLE"){if(vehicle.analysis?.measured!==true)failures.push("SOUND_DESIGN_VEHICLE_ANALYSIS_REQUIRED");if(text(vehicle.source_asset_id)!==text(prep.prepared_asset_id))failures.push("SOUND_DESIGN_VEHICLE_ANALYSIS_SOURCE_STALE");}return{contract:CONTRACT,sound_object_id:soundObject.id||null,source_asset_id:source,prepared_asset_id:text(prep.prepared_asset_id)||null,dry_preparation_current:Boolean(source&&prep.prepared_asset_id&&text(prep.parent_source_asset_id)===text(source)),acoustic_analysis_current:soundObject.category!=="VEHICLE"||Boolean(vehicle.analysis?.measured===true&&text(vehicle.source_asset_id)===text(prep.prepared_asset_id)),immersive_ready:failures.length===0,failures,stage_order:["SOURCE_PROVENANCE","DRY_RESTORATION","SPECTRAL_ACOUSTIC_ANALYSIS","IMMERSIVE_OBJECT_AUTHORING","BINAURAL_OR_SPEAKER_RENDER"],spatial_processing_before_source_prep_forbidden:true,original_source_preserved:true};}
export const CreativeMusicSoundDesignPreparationRuntime=Object.freeze({contract:CONTRACT,evaluate:evaluateMusicSoundDesignPreparation});
