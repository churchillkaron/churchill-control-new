const CONTRACT="AVANTIQO_PROFESSIONAL_AUDIO_LONG_FORM_V1";
const RIFF_SAFE_BYTES=0xff000000; // reserve headroom for metadata/chunks below 32-bit RIFF ceiling
const BROWSER_SAFE_BYTES=1_500_000_000;
function finite(v,f=0){const n=Number(v);return Number.isFinite(n)?n:f;}
export function estimateProfessionalAudioPcmBytes({duration_seconds,sample_rate=48000,channels=2,bit_depth=24}={}){const bytesPerSample=Math.ceil(Math.max(1,finite(bit_depth,24))/8);return Math.max(0,Math.ceil(finite(duration_seconds,0)*Math.max(1,finite(sample_rate,48000))*Math.max(1,finite(channels,2))*bytesPerSample));}
export function planProfessionalAudioLongFormDelivery(input={}){const estimated_pcm_bytes=estimateProfessionalAudioPcmBytes(input),rf64_required=estimated_pcm_bytes>=RIFF_SAFE_BYTES,browser_single_buffer_safe=estimated_pcm_bytes<=BROWSER_SAFE_BYTES;return{contract:CONTRACT,estimated_pcm_bytes,riff_safe_limit_bytes:RIFF_SAFE_BYTES,browser_safe_limit_bytes:BROWSER_SAFE_BYTES,rf64_required,recommended_container:rf64_required?"RF64":"RIFF/WAVE",browser_single_buffer_safe,server_render_required:!browser_single_buffer_safe,segmented_or_streaming_render_recommended:!browser_single_buffer_safe,final_server_wav_rf64_auto:true};}
export const CreativeProfessionalAudioLongFormRuntime=Object.freeze({contract:CONTRACT,estimateBytes:estimateProfessionalAudioPcmBytes,plan:planProfessionalAudioLongFormDelivery});
