import {firingFrequencyFromRpm,mapVehicleRpmToPitch} from "./CreativeVehicleAcousticMathRuntime.js";
const CONTRACT="AVANTIQO_VEHICLE_SYNTHESIS_PLAN_V1";
function finite(v,f=0){const n=Number(v);return Number.isFinite(n)?n:f;}function clamp(v,min,max,f=0){return Math.max(min,Math.min(max,finite(v,f)));}
export function buildVehicleSynthesisPlan({rpm_points=[],cylinders=8,strokes=4,character={}}={}){const points=(rpm_points||[]).map((p,i)=>({time_seconds:Math.max(0,finite(p.time_seconds,i)),rpm:Math.max(0,finite(p.rpm,0))})).sort((a,b)=>a.time_seconds-b.time_seconds);if(points.length<2)throw new Error("VEHICLE_SYNTH_RPM_CURVE_REQUIRED");const c=Math.max(1,Math.round(finite(cylinders,8))),s=Number(strokes)===2?2:4,curve=points.map(p=>({...p,firing_frequency_hz:Number(firingFrequencyFromRpm({rpm:p.rpm,cylinders:c,strokes:s}).toFixed(4)),pitch:mapVehicleRpmToPitch({rpm:p.rpm,cylinders:c,strokes:s})}));return{contract:CONTRACT,cylinders:c,strokes:s,rpm_curve:curve,character:{exhaust_body:clamp(character.exhaust_body,0,1,.65),intake_bite:clamp(character.intake_bite,0,1,.45),mechanical_texture:clamp(character.mechanical_texture,0,1,.25),combustion_aggression:clamp(character.combustion_aggression,0,1,.55),sub_weight:clamp(character.sub_weight,0,1,.35),stereo_width:clamp(character.stereo_width,0,1,.25)},layer_roles:["COMBUSTION_PULSE","EXHAUST_BODY","INTAKE_RESONANCE","MECHANICAL_TEXTURE","SUB_SWEETENER"],intended_use:"FICTIONAL_OR_SWEETENER_LAYER",exact_vehicle_replica_claim:false,measured_vehicle_substitute:false,real_recording_preferred_for_identity:true,source_audio_rewritten:false,automatic_mix_acceptance:false};}
export const CreativeVehicleSynthesisRuntime=Object.freeze({contract:CONTRACT,build:buildVehicleSynthesisPlan});
