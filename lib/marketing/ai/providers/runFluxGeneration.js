export async function runFluxGeneration({

  prompt,

  poster,

}) {

  console.log(
    "FLUX GENERATION PLACEHOLDER",
    {
      engine: poster.engine,
      prompt,
    }
  );

  throw new Error(
    "Flux generation provider not connected yet"
  );

}