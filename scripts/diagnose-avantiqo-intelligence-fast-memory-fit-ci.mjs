const REST_BASE = "https://rest.runpod.io/v1";
const FAST_NAME = "avantiqo-intelligence-fast-v1";
const CONTRACT = "AVANTIQO_INTELLIGENCE_FAST_MEMORY_FIT_V1";
const text=(v)=>String(v??"").trim(); const list=(v)=>Array.isArray(v)?v:[]; const object=(v)=>v&&typeof v==="object"&&!Array.isArray(v)?v:{};
const key=text(process.env.RUNPOD_MANAGEMENT_API_KEY||process.env.RUNPOD_API_KEY); if(!key) throw new Error("RUNPOD_MANAGEMENT_OR_API_KEY_REQUIRED");
async function get(p){const r=await fetch(`${REST_BASE}${p}`,{headers:{Authorization:`Bearer ${key}`,Accept:"application/json"},signal:AbortSignal.timeout(30000)});const raw=await r.text();let b=null;try{b=raw?JSON.parse(raw):null}catch{}if(!r.ok||b===null)throw new Error(`${CONTRACT}_HTTP_${r.status}`);return b;}
function rows(v,ks=[]){if(Array.isArray(v))return v;if(!v||typeof v!=="object")return[];for(const k of [...ks,"data","items","results"])if(Array.isArray(v[k]))return v[k];return[];}
function envMap(v){const pairs=Array.isArray(v)?v.map(e=>[text(e?.key||e?.name),String(e?.value??"")]):Object.entries(object(v)).map(([k,x])=>[k,String(x??"")]);return Object.fromEntries(pairs.filter(([k])=>k));}
const [epsRaw,tmplsRaw]=await Promise.all([get("/endpoints?includeTemplate=true&includeWorkers=false"),get("/templates?includeEndpointBoundTemplates=true&includePublicTemplates=false&includeRunpodTemplates=false")]);const eps=rows(epsRaw,["endpoints","serverlessEndpoints"]);const matches=eps.filter(e=>text(e.name)===FAST_NAME);if(matches.length!==1)throw new Error(`${CONTRACT}_FAST_RESOLUTION_${matches.length}`);const fast=matches[0];const tid=text(fast.templateId||fast.template?.id);const tmpls=rows(tmplsRaw,["templates"]);const t=tmpls.find(x=>text(x.id)===tid)||fast.template||{};const env=envMap(t.env);
const allow=/^(MODEL_NAME|MODEL|MODEL_ID|HF_MODEL_ID|GPU_MEMORY_UTILIZATION|MAX_MODEL_LEN|MAX_NUM_SEQS|MAX_NUM_BATCHED_TOKENS|DTYPE|QUANTIZATION|KV_CACHE_DTYPE|SWAP_SPACE|CPU_OFFLOAD_GB|TENSOR_PARALLEL_SIZE|PIPELINE_PARALLEL_SIZE|VLLM_EXTRA_ARGS|SAFETENSORS_LOAD_STRATEGY|MAX_CONCURRENCY|ENFORCE_EAGER|ENABLE_PREFIX_CACHING)$/i;const selected=Object.fromEntries(Object.entries(env).filter(([k])=>allow.test(k)).sort(([a],[b])=>a.localeCompare(b)));
console.log(JSON.stringify({success:true,contract:CONTRACT,endpoint_id:text(fast.id),template_id:tid,image_name:text(t.imageName)||null,container_disk_gb:Number(t.containerDiskInGb||0)||null,gpu_type_ids:list(fast.gpuTypeIds),allowed_cuda_versions:list(fast.allowedCudaVersions),memory_critical_env:selected,generation_submitted:false,gpu_activation_performed:false,mutation_performed:false,production_deploy_performed:false,secrets_in_output:false},null,2));console.log(`${CONTRACT}=PASS`);
