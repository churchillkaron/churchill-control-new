import { readFile } from "node:fs/promises";
import { resolve } from "node:path";

const BASE = "scripts/provision-avantiqo-video-production-endpoint-v53-local.mjs";

function replaceExactlyOnce(source, search, replacement, label) {
  const count = source.split(search).length - 1;
  if (count !== 1) {
    throw new Error(`AVANTIQO_VIDEO_V61_SOURCE_TRANSFORM_${label}_MISMATCH:occurrences=${count}`);
  }
  return source.replace(search, replacement);
}

let source = await readFile(resolve(process.cwd(), BASE), "utf8");

source = replaceExactlyOnce(
  source,
  `function activeManagementWorkers(endpoint = {}) {\n  return list(endpoint.workers).filter((worker) => {\n    const desired = text(worker?.desiredStatus ?? worker?.desired_status).toUpperCase();\n    const status = text(worker?.status ?? worker?.workerStatus ?? worker?.runtimeStatus).toUpperCase();\n    const effective = desired || status;\n    return Boolean(effective && !["EXITED", "STOPPED", "TERMINATED", "DELETED"].includes(effective));\n  }).length;\n}\n\nfunction assertResting(label, endpoint, health) {\n  const queue = queueSummary(health);\n  const workerTotal = Object.values(queue.workers).reduce((sum, value) => sum + finite(value, 0), 0);\n  const management = activeManagementWorkers(endpoint);\n  if (queue.jobs.in_queue !== 0 || queue.jobs.in_progress !== 0 || workerTotal !== 0 || management !== 0) {\n    throw new Error(\`\${label}_NOT_RESTING:queue=\${queue.jobs.in_queue}:progress=\${queue.jobs.in_progress}:workers=\${workerTotal}:management=\${management}\`);\n  }\n  return { queue, management_non_exited_workers: management };\n}`,
  `function activeManagementWorkerRows(endpoint = {}) {\n  return list(endpoint.workers).filter((worker) => {\n    const desired = text(worker?.desiredStatus ?? worker?.desired_status).toUpperCase();\n    const status = text(worker?.status ?? worker?.workerStatus ?? worker?.runtimeStatus).toUpperCase();\n    const effective = desired || status;\n    return Boolean(effective && !["EXITED", "STOPPED", "TERMINATED", "DELETED"].includes(effective));\n  });\n}\n\nfunction activeManagementWorkers(endpoint = {}) {\n  return activeManagementWorkerRows(endpoint).length;\n}\n\nfunction managementHourlyCost(endpoint = {}) {\n  return activeManagementWorkerRows(endpoint).reduce(\n    (sum, worker) => sum + Math.max(0, finite(worker?.adjustedCostPerHr ?? worker?.costPerHr, 0)),\n    0,\n  );\n}\n\nfunction assertResting(label, endpoint, health) {\n  const queue = queueSummary(health);\n  const queueWorkerCounter = Object.values(queue.workers).reduce((sum, value) => sum + finite(value, 0), 0);\n  const management = activeManagementWorkers(endpoint);\n  const hourlyCostUsd = managementHourlyCost(endpoint);\n  if (queue.jobs.in_queue !== 0 || queue.jobs.in_progress !== 0 || management !== 0 || hourlyCostUsd !== 0) {\n    throw new Error(\`\${label}_NOT_RESTING:queue=\${queue.jobs.in_queue}:progress=\${queue.jobs.in_progress}:queue_health_workers=\${queueWorkerCounter}:management=\${management}:management_hourly_cost_usd=\${hourlyCostUsd}\`);\n  }\n  return {\n    queue,\n    queue_health_worker_counter: queueWorkerCounter,\n    queue_health_worker_counter_authoritative_for_billing: false,\n    management_non_exited_workers: management,\n    management_hourly_cost_usd: hourlyCostUsd,\n    billing_worker_source: "RUNPOD_MANAGEMENT_ENDPOINT_WORKERS",\n    safe_lease_semantics_aligned: true,\n  };\n}`,
  "SAFE_LEASE_IDLE_ACCOUNTING",
);

source = replaceExactlyOnce(
  source,
  `    volumeInGb: Math.max(0, finite(baseTemplate?.volumeInGb, 0)),\n`,
  ``,
  "REMOVE_SERVERLESS_TEMPLATE_VOLUME_IN_GB",
);

source = replaceExactlyOnce(
  source,
  `function templateContract(template = {}) {\n  return JSON.stringify({\n    imageName: text(template.imageName),\n    containerDiskInGb: finite(template.containerDiskInGb, 0),\n    containerRegistryAuthId: text(template.containerRegistryAuthId),\n    dockerEntrypoint: list(template.dockerEntrypoint),\n    dockerStartCmd: list(template.dockerStartCmd),\n    env: normalizeEnv(template.env),\n    isPublic: template.isPublic === true,\n    ports: list(template.ports),\n    volumeInGb: finite(template.volumeInGb, 0),\n    volumeMountPath: text(template.volumeMountPath),\n  });\n}`,
  `function templateContract(template = {}) {\n  const canonicalEnv = Object.fromEntries(\n    Object.entries(normalizeEnv(template.env)).sort(([left], [right]) => left.localeCompare(right)),\n  );\n  return JSON.stringify({\n    imageName: text(template.imageName),\n    containerDiskInGb: finite(template.containerDiskInGb, 0),\n    containerRegistryAuthConfigured: Boolean(text(template.containerRegistryAuthId)),\n    dockerEntrypoint: list(template.dockerEntrypoint),\n    dockerStartCmd: list(template.dockerStartCmd),\n    env: canonicalEnv,\n    isPublic: template.isPublic === true,\n    isServerless: template.isServerless === false ? false : true,\n  });\n}`,
  "SERVERLESS_TEMPLATE_RUNTIME_IDENTITY",
);

source = replaceExactlyOnce(
  source,
  `if (endpointMatches.length > 1) throw new Error(\`\${CONTRACT}_ENDPOINT_AMBIGUOUS:\${endpointMatches.length}\`);`,
  `if (endpointMatches.length !== 1) throw new Error(\`\${CONTRACT}_EXISTING_PRODUCTION_ENDPOINT_REQUIRED:matches=\${endpointMatches.length}\`);`,
  "REQUIRE_EXISTING_PRODUCTION_ENDPOINT",
);

source = replaceExactlyOnce(
  source,
  `if (endpointMatches[0]) {\n  if (!productionTemplate) productionTemplate = resolveTemplate(endpointMatches[0], templates, \`\${CONTRACT}_EXISTING_PRODUCTION\`);\n  assertProductionEndpoint(endpointMatches[0], certification, text(productionTemplate.id));\n  const liveHealth = await queueHealth(text(endpointMatches[0].id), runtimeKey);\n  const restState = assertResting(\`\${CONTRACT}_EXISTING_PRODUCTION\`, endpointMatches[0], liveHealth);\n  console.log(JSON.stringify({\n    success: true,\n    contract: CONTRACT,\n    mode: apply ? "APPLY" : "PLAN",\n    production_endpoint_exists: true,\n    production_endpoint: safeEndpoint(endpointMatches[0]),\n    production_template: safeTemplate(productionTemplate),\n    immutable_image: image,\n    zero_idle_cost_contract: true,\n    idle_gpu_cost_usd_per_hour: 0,\n    resting_state: restState,\n    graphql_multi_volume_objects_verified: true,\n    env_binding: \`RUNPOD_AVANTIQO_VIDEO_PRODUCTION_ENDPOINT_ID=\${text(endpointMatches[0].id)}\`,\n    mutation_performed: false,\n    generation_submitted: false,\n    inference_performed: false,\n    model_download_performed: false,\n    safe_lease_changed: false,\n    image_endpoint_mutated: false,\n    next_action: "BIND_PRODUCTION_ENDPOINT_ENV_AND_RUN_READ_ONLY_ROUTE_INSPECTION",\n  }, null, 2));\n  process.exit(0);\n}`,
  `if (endpointMatches[0]) {\n  const existing = endpointMatches[0];\n  const endpointId = text(existing.id);\n  if (!productionTemplate) productionTemplate = resolveTemplate(existing, templates, \`\${CONTRACT}_EXISTING_PRODUCTION\`);\n  const productionTemplateId = text(productionTemplate?.id);\n  if (!productionTemplateId) throw new Error(\`\${CONTRACT}_EXISTING_TEMPLATE_ID_REQUIRED\`);\n  if (text(existing?.templateId || existing?.template?.id) !== productionTemplateId) {\n    throw new Error(\`\${CONTRACT}_EXISTING_TEMPLATE_BINDING_DRIFT\`);\n  }\n  if (finite(existing?.workersMin, -1) !== 0 || ![0, 1].includes(finite(existing?.workersMax, -1))) {\n    throw new Error(\`\${CONTRACT}_EXISTING_SCALING_UNSAFE:\${finite(existing?.workersMin)}/\${finite(existing?.workersMax)}\`);\n  }\n\n  const fullTemplate = await rest(\`/templates/\${encodeURIComponent(productionTemplateId)}\`, managementKey);\n  if (text(fullTemplate?.id) !== productionTemplateId) throw new Error(\`\${CONTRACT}_FULL_TEMPLATE_ID_MISMATCH\`);\n  if (text(fullTemplate?.imageName) !== image.image) {\n    throw new Error(\`\${CONTRACT}_FULL_TEMPLATE_IMAGE_MISMATCH:\${text(fullTemplate?.imageName) || "MISSING"}\`);\n  }\n  const normalizedFull = { ...fullTemplate, readme: desiredTemplate.readme };\n  if (templateContract(normalizedFull) !== templateContract(desiredTemplate)) {\n    throw new Error(\`\${CONTRACT}_FULL_TEMPLATE_RUNTIME_IDENTITY_MISMATCH\`);\n  }\n\n  const liveHealth = await queueHealth(endpointId, runtimeKey);\n  const beforeRestState = assertResting(\`\${CONTRACT}_EXISTING_PRODUCTION_BEFORE_REPAIR\`, existing, liveHealth);\n  const targetGpuTypeIds = list(certification?.gpuTypeIds).map(text).filter(Boolean);\n  const targetNetworkVolumeIds = endpointVolumeIds(certification);\n  const targetCudaVersions = list(certification?.allowedCudaVersions).map(text).filter(Boolean);\n  const needsRepair =\n    finite(existing?.workersMax, -1) !== WORKERS_MAX ||\n    !sameOrdered(existing?.gpuTypeIds, targetGpuTypeIds) ||\n    !sameSet(endpointVolumeIds(existing), targetNetworkVolumeIds) ||\n    !sameSet(existing?.allowedCudaVersions, targetCudaVersions);\n\n  console.log(JSON.stringify({\n    success: true,\n    contract: CONTRACT,\n    mode: apply ? "APPLY" : "PLAN",\n    repair_kind: "EXISTING_PRODUCTION_ENDPOINT_ONLY",\n    production_endpoint_exists: true,\n    production_endpoint: safeEndpoint(existing),\n    production_template: safeTemplate(fullTemplate),\n    immutable_image: image,\n    serverless_network_volume_runtime_mount: "/runpod-volume",\n    template_volume_mount_path_api_field: text(fullTemplate?.volumeMountPath) || null,\n    template_volume_mount_path_authoritative_for_serverless_network_volume_runtime_mount: false,\n    before_resting_state: beforeRestState,\n    target_workers_min: WORKERS_MIN,\n    target_workers_max: WORKERS_MAX,\n    target_gpu_type_ids: targetGpuTypeIds,\n    target_network_volume_ids: targetNetworkVolumeIds,\n    target_allowed_cuda_versions: targetCudaVersions,\n    repair_required: needsRepair,\n    mutation_performed: false,\n    generation_submitted: false,\n    inference_performed: false,\n    model_download_performed: false,\n    safe_lease_changed: false,\n    certification_endpoint_mutated: false,\n    image_endpoint_mutated: false,\n    next_action: apply ? (needsRepair ? "REPAIR_EXISTING_PRODUCTION_ENDPOINT" : "BIND_PRODUCTION_ENDPOINT_ENV") : (needsRepair ? "APPROVE_V61_REPAIR" : "BIND_PRODUCTION_ENDPOINT_ENV"),\n  }, null, 2));\n\n  if (!apply) process.exit(0);\n  if (!needsRepair) {\n    console.log(\`AVANTIQO_VIDEO_PRODUCTION_ENDPOINT_V61=PASS\`);\n    process.exit(0);\n  }\n\n  const original = {\n    workersMin: finite(existing?.workersMin, 0),\n    workersMax: finite(existing?.workersMax, 0),\n    gpuTypeIds: list(existing?.gpuTypeIds).map(text).filter(Boolean),\n    networkVolumeIds: endpointVolumeIds(existing),\n    allowedCudaVersions: list(existing?.allowedCudaVersions).map(text).filter(Boolean),\n  };\n  let mutationPerformed = false;\n  try {\n    await rest(\`/endpoints/\${encodeURIComponent(endpointId)}\`, managementKey, {\n      method: "PATCH",\n      body: {\n        workersMin: WORKERS_MIN,\n        workersMax: WORKERS_MAX,\n        gpuTypeIds: targetGpuTypeIds,\n        networkVolumeIds: targetNetworkVolumeIds,\n      },\n    });\n    mutationPerformed = true;\n    await rest(\`/endpoints/\${encodeURIComponent(endpointId)}\`, managementKey, {\n      method: "PATCH",\n      body: { allowedCudaVersions: targetCudaVersions },\n    });\n\n    const verified = await fetchEndpointEventually(endpointId, managementKey);\n    assertProductionEndpoint(verified, certification, productionTemplateId);\n    const verifiedTemplate = await rest(\`/templates/\${encodeURIComponent(productionTemplateId)}\`, managementKey);\n    if (text(verifiedTemplate?.imageName) !== image.image) throw new Error(\`\${CONTRACT}_POST_REPAIR_IMAGE_DRIFT\`);\n    const verifiedHealth = await healthEventually(endpointId, runtimeKey);\n    const restState = assertResting(\`\${CONTRACT}_EXISTING_PRODUCTION_AFTER_REPAIR\`, verified, verifiedHealth);\n\n    console.log(JSON.stringify({\n      success: true,\n      contract: CONTRACT,\n      mode: "APPLY",\n      production_endpoint_exists: true,\n      production_endpoint: safeEndpoint(verified),\n      production_template: safeTemplate(verifiedTemplate),\n      immutable_image: image,\n      repaired_existing_endpoint_id: endpointId,\n      repaired_scaling: { workers_min: WORKERS_MIN, workers_max: WORKERS_MAX },\n      repaired_gpu_type_ids: targetGpuTypeIds,\n      repaired_network_volume_ids: targetNetworkVolumeIds,\n      repaired_allowed_cuda_versions: targetCudaVersions,\n      zero_idle_cost_contract: true,\n      idle_gpu_cost_usd_per_hour: restState.management_hourly_cost_usd,\n      resting_state: restState,\n      env_binding: \`RUNPOD_AVANTIQO_VIDEO_PRODUCTION_ENDPOINT_ID=\${endpointId}\`,\n      mutation_performed: true,\n      rollback_performed: false,\n      generation_submitted: false,\n      inference_performed: false,\n      model_download_performed: false,\n      safe_lease_changed: false,\n      certification_endpoint_mutated: false,\n      image_endpoint_mutated: false,\n      secrets_printed: false,\n      next_action: "BIND_PRODUCTION_ENDPOINT_ENV_AND_RUN_READ_ONLY_ROUTE_INSPECTION",\n    }, null, 2));\n    console.log("AVANTIQO_VIDEO_PRODUCTION_ENDPOINT_V61=PASS");\n    process.exit(0);\n  } catch (error) {\n    const rollback = { attempted: mutationPerformed, restored: false, verification_error: null };\n    if (mutationPerformed) {\n      try {\n        await rest(\`/endpoints/\${encodeURIComponent(endpointId)}\`, managementKey, {\n          method: "PATCH",\n          body: {\n            workersMin: original.workersMin,\n            workersMax: original.workersMax,\n            gpuTypeIds: original.gpuTypeIds,\n            networkVolumeIds: original.networkVolumeIds,\n          },\n        });\n        await rest(\`/endpoints/\${encodeURIComponent(endpointId)}\`, managementKey, {\n          method: "PATCH",\n          body: { allowedCudaVersions: original.allowedCudaVersions },\n        });\n        const restored = await fetchEndpointEventually(endpointId, managementKey);\n        rollback.restored =\n          finite(restored?.workersMin, -1) === original.workersMin &&\n          finite(restored?.workersMax, -1) === original.workersMax &&\n          sameOrdered(restored?.gpuTypeIds, original.gpuTypeIds) &&\n          sameSet(endpointVolumeIds(restored), original.networkVolumeIds) &&\n          sameSet(restored?.allowedCudaVersions, original.allowedCudaVersions);\n        if (!rollback.restored) rollback.verification_error = "ORIGINAL_ENDPOINT_STATE_NOT_FULLY_RESTORED";\n      } catch (rollbackError) {\n        rollback.verification_error = redact(rollbackError?.message || rollbackError);\n      }\n    }\n    console.error(JSON.stringify({\n      success: false,\n      contract: CONTRACT,\n      mode: "APPLY",\n      error: redact(error?.message || error),\n      rollback,\n      generation_submitted: false,\n      inference_performed: false,\n      model_download_performed: false,\n      safe_lease_changed: false,\n      certification_endpoint_mutated: false,\n      image_endpoint_mutated: false,\n      secrets_printed: false,\n    }, null, 2));\n    process.exit(1);\n  }\n}`,
  "REPAIR_EXISTING_ENDPOINT_BRANCH",
);

source = source.replaceAll("V53", "V61");
if (source.includes("V53")) throw new Error("AVANTIQO_VIDEO_V61_SOURCE_TRANSFORM_V53_REMAINS");

console.log(`AVANTIQO_VIDEO_V61_SOURCE_TRANSFORM_ACTIVE=${JSON.stringify({
  base: BASE,
  purpose: "REPAIR_ALREADY_EXISTING_VIDEO_PRODUCTION_ENDPOINT_TO_ZERO_IDLE_0_1",
  template_volume_mount_path_policy: "NON_AUTHORITATIVE_FOR_SERVERLESS_NETWORK_VOLUME_RUNTIME_MOUNT",
  serverless_network_volume_runtime_mount: "/runpod-volume",
  idle_billing_authority: "RUNPOD_MANAGEMENT_ENDPOINT_WORKERS_AND_COST",
  target_workers_min: 0,
  target_workers_max: 1,
  rollback_to_original_endpoint_state_on_failure: true,
  creates_endpoint: false,
  creates_template: false,
  generation_submitted: false,
  inference_performed: false,
  model_download_performed: false,
  safe_lease_changed: false,
  certification_endpoint_changed: false,
  image_endpoint_changed: false,
})}`);

const encoded = Buffer.from(source, "utf8").toString("base64");
await import(`data:text/javascript;base64,${encoded}`);
