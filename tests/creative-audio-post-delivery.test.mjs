import test from "node:test";import assert from "node:assert/strict";import fs from "node:fs";
import {buildAudioPostDeliveryPackage} from "../lib/creative/music/runtime/CreativeAudioPostDeliveryRuntime.js";
function session(){return{revision:3,picture_lock:{picture_lock_digest:"lock",frame_rate:24,frame_count:240},spatial_audio:{layout_id:"5.1",channel_count:6},tracks:[{id:"dx",audio_role:"DIALOGUE",clips:[{id:"dx1",source_asset_id:"s1",audio_post:{language:"en",speaker:"A",sync_frame:10,dialogue_edit_approved:true}}]},{id:"mx",audio_role:"MUSIC",clips:[{id:"m1",source_asset_id:"s2"}]},{id:"fx",audio_role:"EFFECTS",clips:[{id:"f1",source_asset_id:"s3"}]}]};}
function stem(code,id){return{id,created_at:"2026-09-18T00:00:00Z",metadata:{music_asset_kind:"PROFESSIONAL_AUDIO_STEM",professional_audio_delivery_code:code,project_revision:3,channels:6,channel_layout:"5.1",peak_dbfs:-3,rms_dbfs:-18,clipping:false,source_assets_preserved:true,destructive_edit:false}};}
test("Audio Post package fails closed until current master and required professional stems exist",()=>{const r=buildAudioPostDeliveryPackage({session:session(),assets:[],language:"en"});assert.equal(r.release_ready,false);assert.ok(r.failures.includes("AUDIO_POST_CURRENT_FULL_MIX_MASTER_REQUIRED"));assert.ok(r.failures.some(x=>x.includes("AUDIO_POST_REQUIRED_STEM_MISSING")));});
test("Audio Post package validates picture lock editorial M&E and current 5.1 assets",()=>{const assets=[stem("DX","dx"),stem("MX","mx"),stem("FX","fx"),stem("ME","me"),{id:"master",metadata:{music_asset_kind:"SURROUND_MASTER",project_revision:3,release_candidate:true,surround_validation_passed:true}}];const r=buildAudioPostDeliveryPackage({session:session(),assets,language:"en"});assert.equal(r.release_ready,true);assert.equal(r.me.asset_id,"me");assert.equal(r.full_mix.asset_id,"master");assert.equal(r.channel_layout,"5.1");});
test("delivery API and UI expose package readiness rather than file-exists semantics",()=>{const route=fs.readFileSync("app/api/creative/music/audio-post-delivery/route.js","utf8"),ui=fs.readFileSync("components/creative/ProductionStudio/workspaces/MusicDeliverablesPanel.jsx","utf8");assert.match(route,/buildAudioPostDeliveryPackage/);assert.match(route,/release_ready/);assert.match(ui,/Audio Post delivery package/);assert.match(ui,/release_ready/);assert.match(ui,/M&E/);});
