import test from "node:test";import assert from "node:assert/strict";
import {buildProfessionalAudioLongFormRenderPlan} from "../lib/creative/music/runtime/CreativeProfessionalAudioLongFormRenderPlanRuntime.js";
test("movie render plan uses sample-exact bodies with DSP context overlap",()=>{const session={revision:7,sample_rate:48000,picture_lock:{picture_lock_digest:"pic"},buses:[{id:"a",effect_type:"reverb",parameters:{decay_seconds:3,pre_delay_ms:20}},{id:"b",effect_type:"delay",parameters:{time_seconds:.5,feedback:.5}}]},release={contract:"RP",project_revision:7,duration_seconds:7200,sample_rate:48000,channels:6,channel_layout:"5.1",speaker_order:["L","R","C","LFE","Ls","Rs"],delivery_profile:{id:"cinema_handoff"}},r=buildProfessionalAudioLongFormRenderPlan({session,release_plan:release,graph_fingerprint:"g"});assert.equal(r.chunk_count,24);assert.ok(r.context_seconds>=3.02);assert.equal(r.chunks[0].body_start_frame,0);assert.equal(r.chunks[0].trim_head_frames,0);assert.ok(r.chunks[1].trim_head_frames>0);assert.equal(r.chunks.at(-1).body_end_frame,r.total_frames);assert.equal(r.final_mastering_after_assembly_required,true);assert.equal(r.chunk_loudness_normalization_forbidden,true);});
test("long-form render plan hash is stable for exact graph and changes with graph identity",()=>{const input={session:{revision:1,sample_rate:48000,buses:[]},release_plan:{project_revision:1,duration_seconds:601,sample_rate:48000,channels:2,channel_layout:"stereo"}};const a=buildProfessionalAudioLongFormRenderPlan({...input,graph_fingerprint:"a"}),b=buildProfessionalAudioLongFormRenderPlan({...input,graph_fingerprint:"a"}),c=buildProfessionalAudioLongFormRenderPlan({...input,graph_fingerprint:"b"});assert.equal(a.plan_hash,b.plan_hash);assert.notEqual(a.plan_hash,c.plan_hash);});
