import test from "node:test";import assert from "node:assert/strict";
import {planProfessionalAudioLongFormStorage,directStorageHostname,tusUploadMetadata} from "../lib/creative/music/runtime/CreativeProfessionalAudioLongFormStorageRuntime.js";
test("movie-size audio uses signed resumable TUS while modest files may use signed PUT",()=>{assert.equal(planProfessionalAudioLongFormStorage({size_bytes:10_000_000}).transport,"SIGNED_PUT");const huge=planProfessionalAudioLongFormStorage({size_bytes:8_000_000_000});assert.equal(huge.transport,"TUS_RESUMABLE");assert.equal(huge.tus_chunk_bytes,6*1024*1024);assert.equal(huge.service_role_on_worker,false);});
test("direct storage host and TUS metadata follow Supabase resumable contract",()=>{assert.equal(directStorageHostname("https://abc.supabase.co"),"https://abc.storage.supabase.co");const meta=tusUploadMetadata({bucket_name:"creative-assets",object_name:"a/b.wav",content_type:"audio/wav"});assert.match(meta,/bucketName /);assert.match(meta,/objectName /);assert.match(meta,/contentType /);});
