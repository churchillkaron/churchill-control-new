import test from "node:test";import assert from "node:assert/strict";import fs from "node:fs";
const runner=fs.readFileSync("services/avantiqo-music-local/singing_voice_runner.py","utf8"),worker=fs.readFileSync("services/avantiqo-music-local/node01_worker.ps1","utf8"),provider=fs.readFileSync("lib/platform/service-runtime/providers/avantiqo-audio/AvantiqoMusicLocalNodeProvider.js","utf8"),audio=fs.readFileSync("lib/platform/service-runtime/providers/avantiqo-audio/AvantiqoAudioProvider.js","utf8"),runtime=fs.readFileSync("lib/creative/music/runtime/CreativeMusicSingingVoiceIdentityRuntime.js","utf8");
test("Seed-VC research runner is consent-bound and preserves singing guide pitch intent",()=>{assert.match(runner,/AVANTIQO_VOICE_REFERENCE_V1/);assert.match(runner,/use_scope.*SINGING/s);assert.match(runner,/--f0-condition", "True"/);assert.match(runner,/--auto-f0-adjust", "False"/);assert.match(runner,/--semi-tone-shift", "0"/);assert.match(runner,/reference_audio_persisted_by_worker.*False/s);});
test("Seed-VC runtime records GPL-3.0 compliance as an explicit production gate",()=>{assert.match(runner,/MODEL_LICENSE = "GPL-3\.0"/);assert.match(runner,/gpl_compliance_review_required.*True/s);assert.match(runner,/production_certified.*False/s);assert.match(provider,/gpl_compliance_review_required: true/);});
test("Node01 advertises singing conversion only when runner checkpoint config and Python runtime exist",()=>{assert.match(worker,/SingingVoiceRuntimeReady = \$false/);assert.match(worker,/SeedVcCheckpoint/);assert.match(worker,/SeedVcConfig/);assert.match(worker,/Test-Path \$SeedVcRunner/);assert.match(worker,/if \(\$SingingVoiceRuntimeReady\).*ai\.audio\.singing-voice-convert/s);assert.match(worker,/RunMusicSingingVoiceIdentityJob/);});
test("owned provider requires explicit research candidate and has no production fallback",()=>{assert.match(audio,/isLocalResearchCapability/);assert.match(audio,/RESEARCH_CAPABILITY_EXPLICIT_CANDIDATE_REQUIRED/);assert.match(audio,/RESEARCH_CAPABILITY_LOCAL_RUNTIME_UNAVAILABLE/);assert.match(audio,/local_only: true/);assert.match(provider,/"ai\.audio\.singing-voice-convert"/);});
test("server runtime resolves Voice Library reference for SINGING instead of accepting arbitrary target URL",()=>{assert.match(runtime,/buildSingingVoiceIdentityExecutionInput/);assert.match(runtime,/resolveVoiceReferenceForExecution/);assert.match(runtime,/purpose: "SINGING"/);assert.match(runtime,/voice_reference: reference\?\.voice_reference/);assert.match(runtime,/gpl_compliance_review_required: true/);});
